import "polymer.lt"  # <-- defines "PolymerAT21"

polymer = new PolymerAT21

# At some point we must indicate the
write_once("Data Boundary") {
  -10.0 10.0   xlo xhi
  -10.0 10.0   ylo yhi
  -10.0 10.0   zlo zhi
}

